const log = require('../logger.js');

module.exports = {
	name: 'template',
	altnames: i18n.__("__alt_cmd__template"),
	description: 'template - do nothing just be there...',
	execute(data, args) {
		//message.channel.send('beep.');
	},
};
